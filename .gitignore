# Database
vendorvault.db
vendorvault.db-wal
vendorvault.db-shm

# Python
__pycache__/
*.pyc
*.pyo

# Environment
.env
venv/
.venv/

# OS
.DS_Store
*.swp
*.swo
*~
Thumbs.db

# Data directory — used by Docker/Railway for persistent SQLite storage
# The db file inside is gitignored; the directory itself is tracked so Docker can mount it
data/*.db
data/*.db-wal
data/*.db-shm

# Reference docs (not part of the app)
VendorVault_Complete_Reference.md

# Video files (large, not part of the app)
vedio clip/
*.mp4
